# 可选：CoinMarketCap API Key。
# 免费版拿不到历史市值（要 Hobbyist 以上套餐），所以历史序列一律走 Yahoo Finance，
# 这个 Key 只用来每天抓一次 BTC 官方实时报价，校验自算供应量的偏差，写入 cmc_snapshot 表。
# 不填则跳过校验，不影响任何图表。
CMC_API_KEY=

# 黄金行情代码，默认 COMEX 黄金期货连续合约。想换现货或 ETF 可改这里。
GOLD_TICKER=GC=F

# 数据库位置，默认 data/market.db
# DB_PATH=
